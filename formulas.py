import math
import numpy

def principal():
	 x = [[50, 42, 80, 70], [32, 29, 15, 19]]
	 w = numpy.zeros((len(x), len(x[0])))

def encontrarK(x1, m1, m2):
	k = argmin(x1, m1, m2)

#Formula 
def calcular(x, m):
	# ||x1 - m1|| = 
	resultado = math.sqrt((x[0][0] - m[0][0]) ** 2 + (x[0][1] - m[0][1]) ** 2)